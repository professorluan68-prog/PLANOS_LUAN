Objetivo: aplicar os patches fornecidos ao repositório PLANOS_LUAN, executar lint e testes unitários, e gerar um relatório de validação.

Tarefas:
1) Aplicar os seguintes arquivos/patches no repositório:
   - core/database.py (substituir inicialização conforme bloco fornecido)
   - adicionar core/cache_manager.py (arquivo completo)
   - adicionar core/ia_client.py (arquivo completo)
   - adicionar core/engine.py (esqueleto)
   - adicionar testes em tests/unit/ conforme fornecido
   - adicionar .github/workflows/ci.yml e scripts em scripts/

2) Executar localmente:
   - black --check .
   - flake8 .
   - pytest tests/unit -q

3) Reportar:
   - Lista de arquivos modificados/novos.
   - Saída do lint (erros/warnings).
   - Resultado dos testes (pass/fail) e stacktrace se falhar.
   - Sugestões de correções rápidas (se houver).
   - Indicar se há secrets hardcoded nos diffs.

Restrições:
- Não alterar lógica de negócio além do necessário para integrar os módulos.
- Não commitar chaves/segredos.
- Se algum teste falhar, gerar um patch corretivo e explicar a causa.

Formato de resposta:
- JSON com campos: modified_files[], lint_ok (bool), tests_ok (bool), test_output (string), suggested_fixes[].
